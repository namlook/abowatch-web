query SubscriptionsList($userToken: String!) {
    user(token: $userToken) {
        username
        subscriptions {
            id
            name
            price
            dailyPrice
            split
            billingMode
        }
    }
}